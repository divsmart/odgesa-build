import HeroSlider from '@/components/HeroSlider';
import UniformOrderSection from '@/components/UniformOrderSection';

export default function HomePage() {
  return (
    <main>
      <HeroSlider />
      <UniformOrderSection />
    </main>
  );
}
